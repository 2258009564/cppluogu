#include <bits/stdc++.h>
using namespace std;
#define int long long

signed main()
{
    int n;
    cin >> n;
    vector<vector<int>> vec(100000, vector<int>(100000, 0));
    for(auto &vec1:vec){
        for(auto &num:vec1){
            cin >> num;
        }
    }
    int x, y;
    cin >> x >> y;
    int num = 1;
    

}